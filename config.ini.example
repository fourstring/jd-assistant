[account]
payment_pwd =

[config]
eid =
fp =
requests_lock = False # 是否在发起检查下单条件请求时加锁。不加锁可有效提高请求并发量，但存在被封禁ip的可能
threads_per_product =1 # 每个商品的线程数，最低为1
# 不建议在不加锁时开启过多线程，可能被京东风控

[pyrogram]
api_id =
api_hash =
bot_token =

[proxy]
enabled = True
hostname = 127.0.0.1
port = 17891

[notification]
enabled = False
target_chat = -123456